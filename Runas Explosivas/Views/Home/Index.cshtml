@using Runas_Explosivas.Models;
@{
    ViewBag.Title = "Runas Explosivas";
    ViewBag.HeaderText = "Tu lugar de referencia sobre juegos de rol en Argentina";
    ViewBag.HeaderGlyph = "";
}

<!-- DISPLAY ARTÍCULOS.PRINCIPAL -->
@*@if (ViewBag.UsuarioRegistrado)
{
    <div class="modal fade" tabindex="-1" role="dialog" data-show="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title">Usuario Registrado!</h4>
                </div>
                <div class="modal-body">
                    <p>Hemos registrado sus datos de usuario exitosamente. Debería recibir un mail a la brevedad. Use el inicio de sesión para conectarse y empezar a comentar artículos, realizar compras, etc.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Cerrar</button>
                </div>
            </div><!-- /.modal-content -->
        </div><!-- /.modal-dialog -->
    </div><!-- /.modal -->
}*@


<div class="container-fluid bottom-pad">
    <div class="main z-depth-1 row">
        <div id="display-articulos">
            <div class="row articulo" data-id="@ViewBag.Articulos[0].ID" id="destacado">
                <h3> @Html.ActionLink(
                linkText: (string) ViewBag.Articulos[0].Titulo,
                actionName: "Articulo",
                controllerName: "Home",
                routeValues: new
                {
                    blogPostId = (int)ViewBag.Articulos[0].ID
                },
                htmlAttributes: null
                ) 
                @ViewBag.Articulos[0].AllGlyphHTML()
                </h3>
                <div class="col-md-10">
                    <p>@ViewBag.Articulos[0].GetPreviewText(300)</p>
                </div>
            </div>
            <hr class="inverted z-depth-1">
            <!-- DISPLAY ARTÍCULOS.SECUNDARIOS -->

            <div class="row" id="display-articulos-secundarios">
                @foreach (Articulo articulo in ViewBag.Articulos)
                {
                    if (articulo.ID != ViewBag.Articulos[0].ID)
                    {
                        <div class="col-md-6 articulo" data-id="@articulo.ID">
                        <h4>@Html.ActionLink(
                            linkText: (string)articulo.Titulo,
                            actionName: "Articulo",
                            controllerName: "Home",
                            routeValues: new
                            {
                                blogPostId = (int)articulo.ID
                            },
                            htmlAttributes: null
                            ) 
                            @articulo.AllGlyphHTML() </h4>
                        <p class="hidden-xs">@articulo.GetPreviewText(200)</p>
                        </div> <!-- fin de row -->
                    }
                }
            </div> 
        </div> <!-- fin de display articulos -->
    </div>
</div>